import { ValidationDirectiveDirective } from './validation-directive.directive';

describe('ValidationDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidationDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
